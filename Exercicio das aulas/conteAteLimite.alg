algoritmo "conteAteLimite"
var
   limite, contador: inteiro
inicio

      escreva("Digite o limite da contagem: ")
      leia(limite)
      
      enquanto (contador <= limite) faca
         escreval(contador)
         contador <- contador + 1
      fimenquanto

fimalgoritmo
