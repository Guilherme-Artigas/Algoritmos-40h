algoritmo "exercicio-46 somando 48 valores"
var
c, soma: inteiro
inicio

      c <- 6
      enquanto (c <= 100) faca
         soma <- soma + c
         c <- c + 2
      fimenquanto
      escreva("Soma de todos os valores:",soma)

fimalgoritmo
